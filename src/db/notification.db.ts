// import { User } from "../models/User.model.js";
// import { getUserByField } from "./auth.db";

// export const getUsersInWaitingRoom = async () => {
//     co getUserByField
//     const user = await User.getUserByFiel();
//     if (!user) throw new Error(errors.userNotFound);
  
//     user.is_confirmed_email = true;
//     await user.save();
// }